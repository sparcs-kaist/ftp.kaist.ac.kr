#!/usr/bin/env geoul-sync
(
cd data
wget $source \
    --no-verbose --timestamping --random-wait \
    --page-requisites --convert-links \
    --recursive --relative --no-parent --no-directories \
    
)

# vim:ft=sh
